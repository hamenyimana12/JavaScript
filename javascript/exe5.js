let tableau = [37, 60, 89, 93, 96, 46, 96, 14, 39, 9];

let nombre = 60
for (let index =0 ; index < tableau.length; index++){
    if (tableau[i]===nombre) {
        
    } ;
   
}
nombre = parseInt(nombre);

if (tableau.includes(nombre)) {
  console.log(`Le nombre ${nombre} existe dans le tableau.`);
} else {
  console.log(`Le nombre ${nombre} n'existe pas dans le tableau.`);
}
